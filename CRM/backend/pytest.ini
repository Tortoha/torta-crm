[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
# Quiet warnings from third-party libs (psycopg2 deprecation, etc.) so
# the test output stays readable.
filterwarnings =
    ignore::DeprecationWarning
    ignore::PendingDeprecationWarning
